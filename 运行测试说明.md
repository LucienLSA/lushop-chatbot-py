# lushop-chatbot-py 运行测试说明

## 0. 最新修复状态（2026-04-09）

已根据 lushop-kratos 项目完成关键对齐，当前项目可启动并通过健康检查。

本次新增/调整：
- 生成 Python gRPC 代码到 src/proto（goods/order/user/inventory）。
- 对齐 gRPC 客户端 Stub：GoodsStub、OrderStub、UserStub、InventoryStub。
- 对齐工具层 RPC 方法：
    - goods：GoodsList / GetGoodsDetail
    - order：OrderList / OrderDetail / UpdateOrderStatus
    - user：GetUserById
    - inventory：InvDetail
- 修复请求字段命名（如 keyWords/pages/pagePerNums/topCategory）。
- 补齐缺失工具文件：inventory_tools.py、user_tools.py、analytics_tools.py。

本次验证结果：
- 模块导入检查通过（src.utils.grpc_client、src.tools.*、src.agent.*、src.api.routes）。
- 启动成功：uvicorn src.api.routes:app --host 127.0.0.1 --port 8100。
- 健康检查成功：GET /health 返回 {"status":"healthy"}。

## 1. 测试结论

截至当前代码状态，项目无法直接正常运行。

主要结论：
- 虚拟环境可用，但运行依赖不完整。
- API 入口模块存在导入失败。
- 业务 Agent 和工具层存在缺失文件与代码引用错误。

## 2. 本次已执行测试

测试时间：2026-04-09
测试目录：/home/ubuntu/lushop-chatbot-py
虚拟环境：.venv

### 2.1 虚拟环境基础检查

执行：
- source .venv/bin/activate
- python -V
- which python
- pip -V

结果：
- Python 3.10.12
- 使用的是项目内 .venv 的 python 与 pip

### 2.2 关键模块导入检查

执行：
- 导入 fastapi, uvicorn, langchain, grpc
- 导入 src.api.routes, src.agent.customer_service, src.agent.user_analyst

结果：
- 成功：fastapi, langchain
- 失败：
  - uvicorn 缺失
  - grpc 缺失
  - src.api.routes 导入失败（由 grpc 缺失引发）
  - src.agent.customer_service 导入失败（由 grpc 缺失引发）
  - src.agent.user_analyst 导入失败（缺少 src.tools.user_tools）

### 2.3 API 启动检查

执行：
- uvicorn src.api.routes:app --host 127.0.0.1 --port 8100

结果：
- 启动失败：系统中无 uvicorn 命令（虚拟环境未安装）

### 2.4 配置项检查

检查 .env 关键项：
- OPENAI_API_KEY
- ANTHROPIC_API_KEY
- XAI_API_KEY
- GOOGLE_API_KEY
- PYLON_API_KEY
- PYLON_KB_ID
- MYSQL_HOST
- GOODS_SERVICE_ADDR

结果：
- 以上键名都存在于 .env

## 3. 阻塞问题清单（代码级）

### 3.1 缺失文件

以下文件在当前仓库中不存在，但被导入：
- src/tools/inventory_tools.py
- src/tools/user_tools.py
- src/tools/analytics_tools.py

证据：
- src/agent/customer_service.py
- src/agent/user_analyst.py

### 3.2 变量未定义

文件 src/agent/customer_service.py 与 src/agent/user_analyst.py 中使用了 configurable_model，但未导入。

### 3.3 导入路径错误

文件 src/tools/goods_tools.py 使用了：
- from src.util.grpc_client import get_goods_client

当前实际目录为 src/utils/，应与 src/utils/grpc_client.py 对齐。

### 3.4 工具文件内部引用不完整

文件 src/tools/goods_tools.py 中直接使用 goods_pb2 与 format_goods_response/format_goods_detail，但未定义或未导入。

文件 src/tools/order_tools.py 中直接使用 order_pb2 与 format_order_response，但未定义或未导入。

## 4. 修复后推荐执行流程

以下流程用于“修复代码后”的正式运行。

### 4.1 安装/补齐依赖

在 .venv 中安装至少以下依赖：
- fastapi
- uvicorn
- langchain
- grpcio
- requests
- python-dotenv

建议命令：
- source .venv/bin/activate
- pip install fastapi uvicorn langchain grpcio requests python-dotenv

### 4.2 修复代码后再启动

建议修复项：
1) 新增缺失文件：
- src/tools/inventory_tools.py
- src/tools/user_tools.py
- src/tools/analytics_tools.py

2) 修正导入与未定义问题：
- 在两个 agent 文件中导入 configurable_model（来自 src/agent/config.py）
- 修正 goods_tools.py 的 src.util 到 src.utils
- 补齐 goods_pb2/order_pb2 与格式化函数引用

### 4.3 启动 API

执行：
- source .venv/bin/activate
- uvicorn src.api.routes:app --host 0.0.0.0 --port 8100 --reload

### 4.4 功能验证

健康检查：
- curl http://127.0.0.1:8100/health

客服接口：
- curl -X POST http://127.0.0.1:8100/api/chat -H "Content-Type: application/json" -d '{"message":"推荐一款苹果","user_id":1}'

分析接口：
- curl -X POST http://127.0.0.1:8100/api/analysis -H "Content-Type: application/json" -d '{"user_id":1,"report_type":"comprehensive"}'

## 5. 当前状态可复现命令（用于自检）

在当前未修复代码下，可执行以下命令复现失败点：

- source .venv/bin/activate
- python - <<'PY'
import importlib
mods = [
    'fastapi', 'uvicorn', 'langchain', 'grpc',
    'src.api.routes', 'src.agent.customer_service', 'src.agent.user_analyst'
]
for m in mods:
    try:
        importlib.import_module(m)
        print('OK', m)
    except Exception as e:
        print('FAIL', m, type(e).__name__, e)
PY

如果你希望，我可以在下一步直接按本说明把缺失工具和导入问题补齐，并把项目修到可启动状态。
